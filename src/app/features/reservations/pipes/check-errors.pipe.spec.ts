import { CheckErrorsPipe } from './check-errors.pipe';

describe('CheckErrorsPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckErrorsPipe();
    expect(pipe).toBeTruthy();
  });
});
